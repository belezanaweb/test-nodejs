import BASE_PATHS from "./basePaths.js";

const PATHS = {
  PRODUCTS: {
    create: `${BASE_PATHS.PRODUCTS}`,
    update: `${BASE_PATHS.PRODUCTS}`,
    delete: `${BASE_PATHS.PRODUCTS}`,
    get: `${BASE_PATHS.PRODUCTS}`,
  },
};

export default PATHS;
